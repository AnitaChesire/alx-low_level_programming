julien@ubuntu:~/c/0x00$ cat main.c 
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int s = 48;
	int r = 97;

	while ( s <= 57 || n <= 102)
	{
		putchar(s);
		s++;
	return (0);
}
