#include "main.h"

/**
  * _abs - finds the absolute value of  an integer
  * @n: passes the value in the funstion
  * Return: 1 success
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
