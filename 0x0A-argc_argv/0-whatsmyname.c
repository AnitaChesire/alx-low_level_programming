#include <stdio.h>
#include <stdlib.h>

/**
  * main - entry point
  * @argc: argument count
  * @argv: arguementin array
  * Return: succes
*/

int main(int argc, char *argv[])
{
	int i = 0;

	if (argc >= 1)
		printf("%s \n", argv[i]);
	return (0);
}
